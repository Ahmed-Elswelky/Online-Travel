import { MintueToHourPipe } from './mintue-to-hour.pipe';

describe('MintueToHourPipe', () => {
  it('create an instance', () => {
    const pipe = new MintueToHourPipe();
    expect(pipe).toBeTruthy();
  });
});
